{
  "schema_version": 1,
  "release": "0.6.3",
  "description": "Bounded installed-artifact release matrix. This contract does not import the twelve landing-page marketing stages.",
  "distribution": {
    "release_targets": [
      "aarch64-apple-darwin",
      "x86_64-apple-darwin",
      "aarch64-unknown-linux-gnu",
      "x86_64-unknown-linux-gnu"
    ],
    "windows_distribution": "deferred_to_future_release",
    "ordinary_windows_ci_required": true
  },
  "groups": [
    {
      "id": "clean-user",
      "requirements": [
        "registration",
        "missing-profile onboarding and consent",
        "first authenticated TUI turn"
      ]
    },
    {
      "id": "returning-user",
      "requirements": [
        "first process exits completely",
        "second installed process reloads credentials without registration",
        "second authenticated TUI turn"
      ]
    },
    {
      "id": "household-grocery",
      "requirements": [
        "active list read",
        "member screening, substitutions, and provenance render",
        "proposal review and edit",
        "cancel without mutation",
        "accept once and advance once",
        "stale list or context authority rejection"
      ]
    },
    {
      "id": "failure-safety",
      "requirements": [
        "no blind retry after uncertain dispatch",
        "accepted slow work emits transport heartbeats within the finite client inactivity deadline",
        "typed inactivity preserves valid partial content and choices and leaves the composer usable without replay",
        "Ctrl-C stream cancellation",
        "Ctrl-C pending-confirmation cancellation without mutation",
        "typed runtime failure remains usable and restores on subsequent clean exit",
        "full presentation restoration after normal and application-interrupt exits",
        "native-signal canonical-mode and body-error or panic restoration remain required companion CI gates"
      ]
    },
    {
      "id": "human-presentation",
      "requirements": [
        "backend stage and tool identifiers render as human progress or neutral fallback copy",
        "restaurant recommendations join evaluated item and member-safety evidence",
        "full menus render prices, provenance, freshness, and dietary explanations",
        "human output never serializes unsupported protocol JSON"
      ]
    },
    {
      "id": "logout-recovery",
      "requirements": [
        "expired channel and session authority refresh and persist before teardown",
        "no refresh or retry occurs after teardown begins",
        "rejected or uncertain preflight clears local authority without teardown dispatch",
        "interrupted uncertain refresh resumes binding-checked local teardown without network dispatch"
      ]
    },
    {
      "id": "artifact-behavior",
      "requirements": [
        "40, 80, and 120 column semantic checks",
        "NO_COLOR",
        "exact archive digest",
        "real platform credential backend on the final signed candidate"
      ]
    }
  ],
  "explicit_non_gates": [
    "native_voice",
    "menu_watch_diff",
    "health_integrations"
  ],
  "canary_or_defer": [
    "menu_watch_management"
  ]
}
